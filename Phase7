🚨 Phase 7 — Failure Testing:-
============================

7.1 — PostgreSQL Pod Failure Test:-
=================================

commands:-

"""
	sudo kubectl get pods -n datawarehouse -o wide 
	
	sudo kubectl delete pods -n datawarehouse -l app=postgres

	sudo kubectl get pods -n datawarehouse -w 
"""

This demonstrates:Failure → Kubernetes detects failure → ReplicaSet creates replacement → PostgreSQL becomes healthy again

7.2 — Worker Failure & Pod Rescheduling:-
=======================================

commands:-

"""
	sudo kubectl get pods -A -o wide 
		
	sudo kubectl cordon cluster-worker2
	
	sudo kubectl uncordon cluster-worker2
	
	sudo kubectl drain cluster-worker2 \
  --ignore-daemonsets \
  --delete-emptydir-data
	
	sudo kubectl get nodes
	
	sudo kubectl get pods -A -o wide
"""

7.3 — Monitoring Failure & Recovery:-
===================================

commands:-

"""
	sudo kubectl get pods -n monitoring -o wide 
	
	sudo kubectl delete pod -n monitoring -l app=prometheus

	sudo kubectl get pods -n monitoring -w

	sudo kubectl logs -n monitoring deploy/prometheus --tail=20

	sudo kubectl get pods -n monitoring
"""


7.4 — Storage & Database Recovery Test:-
======================================

"" We already performed the full backup/restore test in Phase 5, so we should not repeat a destructive database restore. ""

commands:-

"""
	sudo kubectl get pods -n datawarehouse -o wide 

	sudo kubectl exec -it -n datawarehouse deploy/postgres -- \
	pg_isready -U postgres -d DataWarehouse

	sudo kubectl exec -it -n datawarehouse deploy/postgres -- \
	psql -U postgres -d DataWarehouse -c \
	"select count(*) as fact_stock_prices_total from fact_stock_prices;"
	
"""

7.5 — Alert Validation:-
======================

This is the final Phase 7 test.

We need to verify that your Grafana alerts actually detect a failure rather than merely existing.

We'll test the PostgreSQL Down alert using the existing Prometheus metric.

First check the current value:

sudo kubectl exec -n datawarehouse deploy/postgres-exporter -- \
wget -qO- http://localhost:9187/metrics | grep '^pg_up'

Expected:

pg_up 1

Then open Grafana → Alerting → Alert rules.


🎯 Phase 7 — COMPLETE

Your failure-testing results are:

Test	Result
PostgreSQL pod failure & recovery	✅
Worker drain & workload recovery	⚠️ Storage limitation identified
Prometheus failure & recovery	✅
Database integrity after failure	✅ 619,017 rows
PostgreSQL monitoring/alert metric	✅ pg_up = 1
Important engineering finding

Your project now demonstrates a real-world limitation:

Kubernetes can reschedule the PostgreSQL workload, but node-local persistent storage prevents the database from moving to another node.

That's actually excellent material for your project documentation because it shows you tested the architecture instead of only demonstrating the happy path.
